
name: second-workflow
on: 
  push:
    branches:
        - "main"
jobs:
  build: 
    name: run bash command 
    runs-on: ubuntu
    steps:
     - name: run bash command
       run: echo 'second workflow'
       shell: bash
